apiVersion: v1
kind: Service

metadata:
  name: kafka

spec:
  type: NodePort

  selector:
    app: kafka

  ports:

    - name: broker
      port: 9092
      targetPort: 9092
      nodePort: 30092

    - name: controller
      port: 9093
      targetPort: 9093